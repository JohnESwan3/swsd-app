//*
//This page is for an idea, I'm not sure about the practicality of it. In solarwinds, you can export csv files.
// Parsing the files is simple, but what data can we scrape to improve the usefulnes of those charts and graphs?
// This is considered lower priority for now.
// */
export default function Charts() {
    return(
        <div>Charts Page</div>
    )
}